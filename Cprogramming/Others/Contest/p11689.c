#include<stdio.h>
int main()
{
    int e,f,c,sum,d,count;
    count=0;
    for(i=0;i<n:i++)
    scanf("%d %d %d",&e,&f,&c);

        sum=e+f;
        first:
        sum=sum-c+1;
        count++;
        if(sum==1)
            break;
        else
            goto first;


    printf("%d",count);
}
